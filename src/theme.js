import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
  zIndex: {
    mobileStepper: 800,
    speedDial: 850,
    appBar: 900,
    drawer: 950,
    modal: 1000,
    snackbar: 1050,
    tooltip: 1100
  }
})
